#==========================================
# Make sure that headers will be installed
#==========================================

# Make sure that our includes will be copied when the library is installed
INSTALL( DIRECTORY .
         DESTINATION include/ )
